# ===========================================================================
# SLIM API dependency set — what the serverless host (Vercel) installs.
#
# This deliberately OMITS the heavy engine/scientific stack (numpy, pandas,
# scipy, ccxt, Pillow, arrow) so the FastAPI function fits Vercel's serverless
# size limit. The REST API boots fine without them; routers that need the
# engine are skipped on this host (see main._register_routers) and run on the
# persistent host instead.
#
# For the full stack (VPS / Docker / CI) install `requirements-engine.txt`.
# ===========================================================================

# --- Core Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# --- Database & ORM (async) ---
sqlalchemy>=2.0.29
asyncpg>=0.29.0

# --- Authentication & Security ---
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9

# --- Real-Time & Async helpers ---
redis>=5.0.3
httpx>=0.27.0

# --- Supabase (relational app data) ---
supabase>=2.0.0

# --- Object storage (Cloudflare R2, S3-compatible) ---
boto3>=1.34.0

# --- Payments ---
stripe>=8.0.0

# --- AI coaching + email ---
anthropic>=0.25.0
resend>=2.0.0

# --- Middleware, logging & monitoring ---
sentry-sdk>=1.45.0
slowapi>=0.1.9
loguru>=0.7.2
python-dotenv>=1.0.0
