# ===========================================================================
# FULL dependency set — API + trading engine + scientific stack.
# Used by the persistent host (Hetzner VPS / Docker) and CI, where streaming
# and the jesse_custom engine actually run.
#
# The serverless host (Vercel) installs the SLIM `requirements.txt` instead,
# which omits the heavy numpy/pandas/scipy/ccxt/Pillow stack so the function
# fits the serverless size limit. Keep both in sync for shared deps.
# ===========================================================================

# Everything in the slim API set:
-r requirements.txt

# --- Database migrations + sync driver (engine host only) ---
alembic>=1.13.1
psycopg2-binary>=2.9.9

# --- Trading Logic (Jesse engine + scientific stack) ---
numpy>=1.26.4
pandas>=2.2.1
scipy>=1.13.0
arrow>=1.3.0
ccxt>=4.2.0
websockets>=12.0

# --- Image Generation (server-side performance cards) ---
Pillow>=10.0.0

# --- Testing / linting (CI) ---
pytest>=8.1.1
pytest-asyncio>=0.23.6
ruff>=0.3.0
black>=24.3.0
